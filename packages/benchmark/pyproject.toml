[project]
name = "cashproof-benchmark"
version = "0.1.0"
description = "CashProof benchmark runner, synthetic dataset generator, and evaluator"
requires-python = ">=3.12"
dependencies = [
    "cashproof-domain",
    "cashproof-application",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cashproof"]
