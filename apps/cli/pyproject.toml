[project]
name = "cashproof-cli"
version = "0.1.0"
description = "CashProof CLI composition root"
requires-python = ">=3.12"
dependencies = [
    "cashproof-domain",
    "cashproof-application",
    "cashproof-infrastructure",
    "cashproof-ai",
    "cashproof-benchmark",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cashproof"]
